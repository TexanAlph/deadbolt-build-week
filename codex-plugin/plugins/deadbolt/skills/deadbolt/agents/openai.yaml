interface:
  display_name: "Deadbolt"
  short_description: "Audit and triage code security risks honestly"
  brand_color: "#B7F34A"
  default_prompt: "Use $deadbolt to audit code I own read-only, cite evidence, and recommend fixes without applying them."
policy:
  allow_implicit_invocation: true
