interface:
  display_name: "Deadbolt"
  short_description: "Audit code you own with evidence, not guesses"
  brand_color: "#B7F34A"
  default_prompt: "Use $deadbolt to perform a read-only security audit of code I own and cite evidence for every finding."
policy:
  allow_implicit_invocation: true
